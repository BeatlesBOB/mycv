{% extends 'base.html.twig' %}

{% block title %}Realisations{% endblock %}

{% block body %}
    <h1>Realisations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ realisation.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ realisation.Title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ realisation.Description }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ realisation.Url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('realisations_index') }}">back to list</a>

    <a href="{{ path('realisations_edit', {'id': realisation.id}) }}">edit</a>

    {{ include('realisations/_delete_form.html.twig') }}
{% endblock %}
