{% extends 'base.html.twig' %}

{% block title %}Lv3{% endblock %}

{% block body %}
    <h1>Lv3</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lv3.id }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ lv3.niveau }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lv3_index') }}">back to list</a>

    <a href="{{ path('lv3_edit', {'id': lv3.id}) }}">edit</a>

    {{ include('lv3/_delete_form.html.twig') }}
{% endblock %}
