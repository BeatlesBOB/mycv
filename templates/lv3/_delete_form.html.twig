<form method="post" action="{{ path('lv3_delete', {'id': lv3.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lv3.id) }}">
    <button class="btn">Delete</button>
</form>
